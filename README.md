# Music Theory Reference

Generate PDF references of basic music theory concepts.

## Run It

Settings are stored in .env.

Run the docker container to generate all the PDF reference files (and their Lily Pond source files) in [./$APP_PATH/render](./generator/render).

```shell
docker compose run renderer
```
